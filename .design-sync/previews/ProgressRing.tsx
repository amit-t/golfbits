import { ProgressRing } from "@golfbits/design-system";

export const Partial = () => <ProgressRing pct={68} />;
export const Full = () => <ProgressRing pct={100} />;
export const Large = () => <ProgressRing pct={42} size={72} />;
